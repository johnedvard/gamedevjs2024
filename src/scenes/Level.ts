import { SceneKey } from '~/enums/SceneKey';

export class Level extends Phaser.Scene {

  constructor() {
    super(SceneKey.Level);
  }
  preload(): void { }
  create(): void { }
  update(): void { }
}
